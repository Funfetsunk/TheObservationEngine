DATABASE_URL="postgresql://postgres:password@localhost:5432/wixbury"
REDIS_URL="redis://localhost:6379"

# Override tick speed for local dev (default is 60000 = 1 real min per sim hour)
# Set to 100 for fast runs during development
TICK_INTERVAL_MS=100
